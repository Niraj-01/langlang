import { Landing } from "@/components/Landing";

// The public home page. The app itself lives at /reels (feed),
// /learn (dashboard) and friends — this is the front door.
export default function Home() {
  return <Landing />;
}
